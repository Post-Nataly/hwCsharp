// программу, которая на вход принимает число (N), а на выходе показывает все чётные числа от 1 до N.

int N = 15;
int count = 0;

while (count < N)
{
Console.Write(count);
Console.Write(", ");
    count +=2;
}
Console.Write(N);
